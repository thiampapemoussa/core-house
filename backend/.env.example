# ── Port du serveur ──
PORT=3001

# ── Google Calendar (Service Account) ──
GOOGLE_CALENDAR_ID=
GOOGLE_CLIENT_EMAIL=
GOOGLE_PRIVATE_KEY=

# ── Email (Gmail SMTP avec App Password) ──
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=core21.lab@gmail.com
EMAIL_PASS=
EMAIL_FROM=Core House Dakar <core21.lab@gmail.com>

# ── Frontend origin (CORS) ──
FRONTEND_URL=http://localhost:8090

# ── Site URL (for QR codes) ──
SITE_URL=http://localhost:8090

# ── Staff PIN (check-in & admin access) ──
STAFF_PIN=0000
